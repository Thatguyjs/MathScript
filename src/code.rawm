// Single line comment

/*
	Multi-line comment
*/


int myInt;
string myString = "abc";
bool myBool = true;

myInt = 5;


if(true && 10 > 0) {
	print("Hello, Program!");
}
else if(myInt + 10 > 20) {
	print("Goodbye, Program!");
}


int returnFive = function(depth) {
	if(depth == 5) {
		return depth;
	}
	else {
		return returnFive(depth + 1);
	}
}

returnFive(0);
